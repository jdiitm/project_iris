[
 {iris_core, [
    {join_seeds, []},
    {auto_init_db, true},
    {allow_schema_delete, true},
    %% AUDIT FIX: Expected cluster nodes for partition detection
    {expected_cluster_nodes, []}
 ]},
 {iris_edge, [
    {core_nodes, []},
    %% AUDIT FIX: TLS is MANDATORY per RFC NFR-14
    %% This config enforces TLS for all client connections
    {allow_insecure, false},
    {tls_enabled, true},
    {tls_certfile, "certs/test_server.pem"},
    {tls_keyfile, "certs/test_server.key"}
 ]}
].
